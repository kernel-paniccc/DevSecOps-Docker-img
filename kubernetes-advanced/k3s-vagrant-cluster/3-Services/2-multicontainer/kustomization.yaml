apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 2-multicontainer.yml
configMapGenerator:
  - name: my-nginx-conf
    files:
      - default.conf=nginx-default.conf
      - index.html=nginx-index.html
